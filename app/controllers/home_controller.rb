## frozen_string_literal: true
class HomeController < ApplicationController
  # This controller serves the home page of the application.
  # It currently does not have any specific actions defined.
  # You can add more actions as needed for your application.

  # NOTE: Normally this page would be static
  # but no time to generate a static file with the search bar
  def index
  end
end
